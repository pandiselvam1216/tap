[build]
  command = "npm run build"
  publish = "dist"

# Netlify redirects to solve CORS and proxy API calls to Render
[[redirects]]
  from = "/api/*"
  to = "https://faucet-vision-api.onrender.com/api/:splat"
  status = 200
  force = true
